<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@drawable/shape_item_background" /> <!-- Your item background shape -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="8dp" /> <!-- Adjust corner radius as needed -->
            <solid android:color="@android:color/transparent" />
            <padding
                android:bottom="2dp"
                android:left="2dp"
                android:right="2dp"
                android:top="2dp" />
            <stroke
                android:width="1dp"
                android:color="@color/shadow_color" /> <!-- Shadow color -->
        </shape>
    </item>
</layer-list>